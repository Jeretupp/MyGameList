<%- include('header'); -%>

<main>
	<div class="container">
		<% game.forEach(data => { %>
		<ul class="game">
			<li><img src="<%= data.image %>" alt="<%= data.name %>" class="game-img" /></li>
			<li>
				<h1><%= data.name %></h1>
			</li>

			<li>
				<a href="/<%= data.id %>">View Game &#x21DB;</a>
			</li>
		</ul>
		<% }) %>
	</div>
</main>
<%- include('footer'); -%>
