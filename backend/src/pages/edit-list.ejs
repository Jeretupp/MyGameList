<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>
            <% if(editing){ %>Edit Game<% } else { %>Add Game<% } %>
        </title>
    </head>
    <body>
        <h1><% if(editing){ %>Edit Game<% } else { %>Add Game<% } %></h1>

        <form method="POST" action="/<% if(editing){ %>edit-list<% } else { %>add-game<% } %>">
            <div>
                <label>Name</label>
                <input type="text" name="name" value="<% if(editing){ %><%= game.name %><% } %>" required />
            </div>
            <div>
                <label>Image</label>
                <input type="text" name="image" value="<% if(editing){ %><%= game.image %><% } %>" />
            </div>
            <div>
                <label>Description</label>
                <input type="text" name="description" value="<% if(editing){ %><%= game.description %><% } %>" required />
            </div>
            <div>
                <label>playtime</label>
                <input type="text" name="playtime" value="<% if(editing){ %><%= game.playtime %><% } %>" required />
            </div>
            <% if(editing){ %>
            <div>
                <input type="hidden" name="gameId" value="<%= game.id %>" />
            </div>
            <% } %>
            <div>
                <button type="submit"><% if(editing){ %>Edit Game<% } else { %>Add Game<% } %></button>
            </div>
        </form>
    </body>
</html>
